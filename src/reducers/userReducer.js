const initialState = {
  userSettings: {
    // Replace with own settings
  }
};

const userReducer = (state = initialState, payload) => {
  switch (payload.type) {
    default:
      return state;
  }
};

export default userReducer;
